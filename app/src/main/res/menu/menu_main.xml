<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.example.testapp.MainActivity">
    <item
        android:id="@+id/add_settings"
        android:orderInCategory="100"
        android:title="@string/add_settings"
        app:showAsAction="never" />
    <item
        android:id="@+id/editable_settings"
        android:orderInCategory="100"
        android:title="@string/editable_settings"
        app:showAsAction="never" />
    <item
        android:id="@+id/search_settings"
        android:orderInCategory="100"
        android:title="@string/search_settings"
        android:icon="@drawable/ic_search"
        app:showAsAction="collapseActionView|ifRoom"
        app:actionViewClass="android.support.v7.widget.SearchView" />
</menu>
